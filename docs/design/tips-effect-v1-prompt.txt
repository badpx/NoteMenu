生成方式：Codex 内置 image_gen。先基于 build/design-preview/zh-Hans-light-empty-380.png 生成，再基于首版图调整弹层位置。

初版提示词：
Use case: ui-mockup. Asset type: one polished, high fidelity macOS UI design board for the NoteMenu Tips specification. The supplied image is the EDIT TARGET and authoritative visual reference for the NoteMenu app window. Create one wide image showing TWO SEPARATE UI STATES side by side on a very light neutral desktop canvas, with ample spacing, each window closely matching the reference screenshot in dimensions, typography, icon shapes, light colors, rounded corners, blank editor area, bottom formatting toolbar and yellow brand accent. State A on the left: an empty NoteMenu window, preserve its placeholder text '现在的想法是…' and all existing controls; add ONLY a short, subtle single-line titlebar hint in the empty space between 'NoteMenu' and the pin control, with exact text '⌘↵ 存到备忘录'. This hint must fit without covering the title or pin/close controls, and the editor canvas remains completely empty. State B on the right: the same NoteMenu window with no titlebar hint; show a compact native macOS light-mode Tips popover OUTSIDE the editor window, anchored to a small NoteMenu menu-bar icon above it, never covering the typing area. The popover title is exactly '使用技巧' and it has three concise, readable rows with exact text: '快速保存  ⌘↵ 存到备忘录', '分级全选  连续按 ⌘A', '列表层级  Tab / ⇧Tab'. Show the states as alternatives, not simultaneous UI on one window. Design quality: crisp real product screenshot, 2x macOS retina rendering, restrained Apple Notes-adjacent visual language, accurate Simplified Chinese typography, no decorative illustrations, no extra buttons, no extraneous labels, no watermarks. Preserve reference window content and controls as faithfully as possible.

位置调整提示词：
Edit this NoteMenu Tips design board. Keep the two NoteMenu app windows, their contents, colors and typography substantially the same. Fix the composition of the RIGHT scenario: move the right app window LOWER on the canvas so that the entire '使用技巧' popover fits ABOVE the app window, with a clear 24px vertical gap between the popover bottom and the app window top. The popover must be wholly outside the app window and cannot cover its title bar or white editing area. Keep the popover anchored visually to the small yellow menu-bar icon at upper right. Increase vertical canvas height as needed to fit the right window. Keep the left example visually distinct, with the titlebar hint. Keep exact Simplified Chinese labels already visible. This is a high fidelity macOS product mockup, no annotations, no extra text, no watermarks.
