Use case: ui-mockup.
Final correction pass: align left draft heading baseline with right heading, allowing top card to visibly occlude it without reflow; caret on final closing-brace line; remove save bubble triangle and darken hovered save button. Preserve everything else.
Edit target/reference: attached NotesMate Tips board. Create a revised high-fidelity presentation with FOUR frontal app windows, two columns and two rows. Preserve the approved native NotesMate app icon, header, editor and footer skeleton, exact restrained yellow/neutral palette, typography and equal toolbar hit targets. Light row top, dark row bottom. Landscape board, crisp Chinese, neutral background.
Title "NotesMate · Tips V2", subtitle "提示聚焦上方，正文布局不动".
Column labels "01  顶部编辑小贴士" and "02  保存快捷键". Row labels "浅色" and "深色".
Every window: yellow actual NotesMate icon + NotesMate at header left, pin/close at right; footer # Aa bullet list numbered list image, separator, folder 会议纪要 with chevron, yellow save write button. No extra toolbar tooltips, no permanent folder background, no hover on Aa.
LEFT column: show editing draft containing a heading near top and a small monochrome code block lower down with func greet() { / print("Hello") / }. At exactly 8pt below the header bottom edge, centered horizontally, show a floating pale-yellow context card max300pt width and roughly40pt height, with small lightbulb, exact text "在代码块最后一行按 ↓，可回到正文。", small close x. This is an OVERLAY above the editor, NOT an inserted row. It may occlude the top draft heading behind it, with that heading remaining at its original y-position; do NOT move document content down or add reserved blank space. Code below remains visible. Visible caret at end of lower code line stays unobscured. The card is detached from header by8pt, no arrow, corner8pt, light shadow. Light card #FFF8DE / text #5C4A21 / border #E8DCA8. Dark card #302C21 / text #E6D9B5 / border #51462A. Same card width in light/dark.
RIGHT column: normal text draft "随手记下" and "把零散的想法，留给下一次思考。" at usual top editing origin, no contextual card at top, no saved status. Pointer hovers save at lower right, save darkens yellow. A compact custom warm tooltip floats 6pt ABOVE save button, right edge aligned to save button, exact text "存至备忘录" followed by keycaps "⌘" and "↩". Single row, 12pt,8ptcorners,10pthorizontalpadding, no title/body stack or pointer triangle. Same warm pale-yellow/darkcharcoal palette as contextcard, subtle outline/shadow, no native system tooltip. Nothing near other controls.
Caption under left column "标题栏下方 8pt · 最大宽度 300pt · 不占正文空间".
Caption under right column "悬停 150ms 显示 · 移开即隐藏".
Footer board caption "开始输入、选择或滚动时，小贴士立即收起".
Shared light editor white/chromeFAFAF8, darkeditor202122/chrome28292A, saveFBD32E, text323232/E8E8E5. Preserve all invariants. No syntax highlighting, no blue, no success banner, no permanent new toolbar row, no bottom context card. Keep entire windows readable and accurately aligned.
