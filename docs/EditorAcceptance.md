# NotesMate 编辑器验收矩阵

对应 [EditorSpec.md](EditorSpec.md) 与 [EditorDesign.md](EditorDesign.md)。D1–D12 已写入规范 §11。本文件保留完整验收预期；2026-09-20 的执行记录、自动化覆盖和真机待验项见 [EditorAcceptanceResults.md](EditorAcceptanceResults.md)，不能将矩阵本身视为通过记录。

## 1. 测试组织

测试分为三个必要层次，复用同一组操作序列和模型 fixtures。

| 层次 | 验证对象 | 验收内容 |
|---|---|---|
| Core 单元/性质测试 | Document、Reducer、PositionMap、TriggerEngine、ListResolver、Exporter | 状态转换、Unicode 范围、编号、HTML 结构、撤销前后快照 |
| AppKit 集成测试 | 真实 NSTextView/TextKit、UndoManager、NSPasteboard 测试实例 | 原生 delta、typing 属性、布局、selection、粘贴来源和历史分组 |
| 真机交互 | 实际中文输入法、窗口、系统备忘录 | 候选窗定位、快捷键分派、排水区点击/拖选、缩放、最终 Notes 输出 |

测试 fixture 至少记录：初始文档、初始选区、输入来源、事件序列、最终段落/marks、选区、输入意图、编号、HTML、undo/redo 后状态。视觉断言以相对于正文起点的坐标为主，避免把系统字体抗锯齿差异当作语义失败。

Swift Package 测试 target 覆盖 Core、AppKit 集成和持久化；Xcode 工程保留应用 target，不应把 `xcodebuild build` 当成这些测试已执行。真机 IME 不能由直接调用 `insertText` 的测试替代。

## 2. 文档和范围

| ID | 操作/输入 | 预期 |
|---|---|---|
| M01 | 初始空编辑器 | 一个空 body 节点；投影长度 0；selection 合法 |
| M02 | 空文档转列表 → 输入 → 删光文字 | 同一个列表节点继续存在，不能自动变正文 |
| M03 | `a` → Enter → 文末列表按钮 | 最后一个空段成为列表；前面的 a 不受影响 |
| M04 | 光标在 `a\n` 的 EOF | 映射到末尾零长度段落，不能落到 a 段 |
| M05 | 单字符列表 `a` 按 Enter | 按非空项续排，不能因 paragraphRange.length == 1 退出 |
| M06 | 附件单独成项 | 属于非空项；Enter 续排 |
| M07 | 含 emoji、组合音符、中文的选区和删除 | 不拆代理对、组合字符或附件；UTF-16 范围正确 |
| M08 | 跨段落选区结束于下一段 offset 0 | 按 D9 不误处理下一段 |
| M09 | 反向选择、拖选跨列表/标题/正文 | anchor/head 与被覆盖段落正确 |
| M10 | 全选替换、跨段 cut、默认 Delete 合并段落 | 结构无丢失；undo 恢复原段落 ID/类型/文本 |

## 3. 触发与输入格式

| ID | 操作/输入 | 预期 |
|---|---|---|
| T01 | 逐键输入 `# ` / `## ` / `### ` | 对应空标题节点；标记消失；光标留在正文起点 |
| T02 | `- ` / `* ` / `1. ` / `99. ` | 列表转换；有序起号均为 1 |
| T03 | 三反引号后输入空格 | 转代码行 |
| T04 | `#### ` / `0. ` / `100. ` / `> ` / `--- ` | 不转换，保留原文 |
| T05 | 非段首，或标记前有空格 | 不触发块级转换 |
| T06 | 在已有列表中输入标题/列表/代码块标记 | 不触发块级转换；原列表节点保持 |
| T07 | `**粗体**` / `__粗体__` / `*斜体*` / `_斜体_` / `~~删除~~` / 单反引号配对 | 对应 mark；两端标志删除 |
| T08 | 标志内容为空、首尾空白、内部含同标记符 | 不转换 |
| T09 | `abc_def_` | 开口前不符合边界，不转换 |
| T10 | 中文标点后输入合法配对 | 按 Unicode 标点正确识别 |
| T11 | 逐键输入 `**a**`，停在第一枚闭合星号时观察 | 不被单星号提前解释成斜体 |
| T12 | 行内转换后立即输入中文、英文、空格与 emoji | 新内容不继承刚刚生成的行内格式 |
| T13 | 转换后 selection 通知、布局刷新、窗口缩放，再继续输入 | 显式复位意图不被系统属性继承覆盖 |
| T14 | 工具栏主动开启粗体后连续键入 | 继续使用粗体；与自动转换后的复位区别明确 |
| T15 | Undo 恢复带标志文本 → 点击/重排布局 | 不自动重新转换 |
| T16 | 闭合标志替换一个选区，或系统一次提交多个字符 | 基于真实 replacementRange 和来源判定，无旧下标越界 |
| T17 | 代码区域输入配对、跨段配对、三连标记、`01.` | 按 D3/D5 固定预期 |
| T18 | 逐键输入 `- [ ]`；另纯文本粘贴同一串 | 按 D12 前者是普通列表及文字，后者整串保持文字；不产生 task 语义 |

## 4. 键盘与选区格式

| ID | 操作 | 预期 |
|---|---|---|
| K01 | 正文 Enter | 新正文段 |
| K02 | H1/H2/H3 的末尾及中间 Enter | 新右侧段正文，左侧保留标题 |
| K03 | L1/L2/L3 有文字项 Enter | 新同层列表；编号续排 |
| K04 | L3 空项连续 Enter 三次 | L2 → L1 → 正文；三次均不插换行 |
| K05 | L1 空项连续 Tab 至 L8，再 Tab | 逐级增加至 L8 后保持 L8；最后一次不产生无意义 Undo |
| K06 | 有文字列表与空列表执行同样 Tab 序列 | 类型/层级状态转换一致 |
| K07 | 一级列表 Shift+Tab | 原地退出列表 |
| K08 | 普通文本/标题 Tab、Shift+Tab | Tab 在光标处插入制表符或替换选区；Shift+Tab 无操作，不跳焦点 |
| K08b | 代码块行中、空行、多行选区 Tab/Shift+Tab | 整行行首增减缩进，光标随原文本移动；撤销重做正确；选区结束于下一行首时不影响该行 |
| K09 | 标题/代码行段首 Backspace | 首次转正文不删字；再次走默认删除 |
| K10 | L3/L2/L1 段首 Backspace | 提升一层或退出，不删字 |
| K11 | 列表视觉折行起点 Backspace | 普通删字，不能误判为段落起点 |
| K12 | 非空选区 Backspace | 删除选区，不先降级格式 |
| K13 | 非空代码行 Enter、空代码行 Enter | 续代码行/退出为新正文；空行保留按 D4 |
| K14 | ⌘B/⌘I/⌘U/⇧⌘X，空选区及跨段非空选区 | 空选区只改行内输入意图；有选区只改区间字符 |
| K15 | 块菜单和列表按钮，跨多个段落 | 覆盖全部目标段落，包括中间空段 |
| K16 | 混合 marks 或 mixed list 选区 toggle | 按 D10 统一开关，不能逐 run 反转产生相反混合态 |
| K17 | 主键盘 ⌘Return、小键盘 ⌘Enter、发送按钮 | 同一保存命令，防止一个事件发送两次 |

## 5. 列表与排版

| ID | 场景 | 预期 |
|---|---|---|
| L01 | L1–L8 无序列表 | 缩进为 22×层级，最大 176；标记依次 ●/○/◆/◇/■/□/▲/△ |
| L02 | `L1 A, L2 B, L2 C, L1 D, L2 E` | 按 D6 编号 `1,1,2,2,1` |
| L03 | 同层有序/无序切换、正文中断后再次列表 | 按列表组重启规则编号 |
| L04 | 首项 Tab 到 L3、L1 后直接出现 L3 | 编辑模型保留规定层级；HTML 按 D7 裁定，不能输出无效嵌套 |
| L05 | 项中有长文本、英文单词、emoji、附件及混合字号 | 后续折行缩进一致；仅首行有一个标记；基线正确 |
| L06 | 编号达到 9/10/99/100 项 | 正确右对齐；不能为了容纳编号自动修改定稿缩进 |
| L07 | 初始空列表、文末空列表、中间空列表 | 均显示标记，caret 位于正文起点之后 |
| L08 | 点击排水区、拖选、左右/上下/Home、撤销恢复 | caret 不进入符号左侧；拖选 anchor 保持 |
| L09 | 滚动长文档、缩放到最小窗口、Retina 显示 | 标记/正文/命中区域坐标一致，不在每次 draw 全文扫描 |
| L10 | 正文、标题、代码和空白段落交替 | 字号、字体、行距 4、段间距 0；自然折行不增节点 |
| L11 | 初始占位、激活空标题/列表 | 文案与显示条件按 D9 |
| L12 | 只改变字体或缩进后重新布局 | 候选窗锚点与实际插入位置一致 |

L06 若三位以上编号宽度超过固定排水区，应记录实际视觉结果并回到规范裁定，不以裁剪编号或擅自加大缩进当作通过。

## 6. 输入法和剪贴板

| ID | 场景 | 预期 |
|---|---|---|
| I01 | 拼音组合中出现星号、空格、回车 | 不触发 Markdown，交给输入法 |
| I02 | markedRange replacement 提交，提交后 hasMarkedText 变 false | 仍依据提交前状态跳过触发 |
| I03 | IME 中点击段落/字体/列表按钮或按 ⌘B/⌘I/⌘U/⇧⌘X；另测 Tab/Shift+Tab/Enter/⌘Return | 主动格式操作先保留当前文本并结束组合，再应用格式，支持撤销及继续中文输入；选词按键和发送快捷键仍交给 IME，不误触发列表/发送 |
| I04 | 组合取消、重新选择候选、选区内重转换 | 原结构、已提交文本和历史一致，无拼音中间态草稿 |
| I05 | 列表空项开始组合，滚动/缩放后继续 | 缩进、候选窗和最终光标一致 |
| I06 | 组合中点击发送按钮 | 系统提交完成后才导出一次快照 |
| P01 | 纯文本粘贴 `- `、`**粗体**`、三反引号及多段文本 | 原样保留，不做 Markdown 转换 |
| P02 | 富文本粘贴不同字体、16/17/22/23pt、等宽字体 | 分别按 14/18/18/24 与 Courier 12 归一 |
| P03 | 富文本粗/斜/下划线/删除线、三级混合列表 | 保留允许语义；列表深度处理一致 |
| P04 | 剪贴板同一图同时含 PNG/TIFF | 只插一张，优先 PNG |
| P05 | 无法解码的 PNG、有可解码 TIFF | 使用 TIFF，不静默丢掉整次粘贴 |
| P06 | Finder 复制多个图片文件 | 每文件一张、顺序一致、一次 Undo 全部撤回 |
| P07 | RTFD 含文本与多图，拖入图片 | 图片不重复；真实数据入库；按用户插入位置混排 |
| P08 | 图片缩略、删除、Undo、Redo、草稿重启恢复 | 高度 ≤72；原始数据不丢；附件数始终匹配文档 |
| P09 | 粘贴链接、颜色、任意缩进、私有属性 | 文字保留，规范外格式不进入模型 |
| P10 | 粘贴/替换后首次直接输入空格或闭合符 | 只评估此次直接键入的上下文，不补跑全文粘贴转换 |
| P11 | 应用内复制/剪切混合列表、空项、行内格式和图片后粘贴 | 通过模型交换片段保持结构；不因显示投影省略 textLists 而丢列表 |

## 7. 历史与草稿

| ID | 操作 | 预期 |
|---|---|---|
| U01 | 合法标志触发 → Undo → Redo | 转换一步撤销/重做；字面标志预期按 D8；模型/caret/标记一致 |
| U02 | 普通连续键入含未命中标记 | 与原生 NSTextView 的连续输入撤销合并一致，不能每字符一个组 |
| U03 | 连续键入 → 自动转换 → 继续键入 → 连续 Undo | 各阶段边界清楚，不能只撤字体而留删标记结果 |
| U04 | 空列表项 Tab/Shift+Tab、行内输入属性 toggle | 各动作一个组，零字符变化也能恢复 |
| U05 | 普通键入、结构事务、粘贴、删除交错 | 同一个历史栈顺序正确，没有重复撤销同一修改 |
| U06 | 组合提交/取消、跨段落替换再 Undo/Redo | 不暴露半提交 marked text；结构恢复完整 |
| U07 | Undo 后执行新编辑 | 旧 redo 分支失效，asset 回收不影响仍可撤销的附件 |
| U08 | 保存成功清空/保存失败 | 前者按 D10 新会话，后者原稿与历史保留 |
| D01 | 只改工具栏格式，不再输入，关闭再打开 | 变化已持久化 |
| D02 | 空列表/空标题关闭重启 | 恢复格式节点，不能按“无法发送”删除草稿 |
| D03 | 防抖任务未写完时清空或发送成功 | 旧任务不能把旧草稿写回 |
| D04 | 新旧 revision 写入耗时倒序 | 恢复始终是最新已提交版本 |
| D05（已退役） | 发布前 RTFD 草稿迁移 | 正式 v1.0 已使用 JSON，无线上 RTFD 草稿；兼容路径及对应测试已移除，保留编号用于历史记录 |
| D06 | 文档包中途写入失败、缺 asset、损坏 manifest | 保留可恢复版本/原文件，不静默清空覆盖 |
| D07 | 图片删除后仍可 Undo，再重做并清空历史 | asset 生命周期正确，既不提前丢图也不永久泄漏 |

## 8. HTML 与保存

| ID | 文档 | 预期 |
|---|---|---|
| E01 | 首段正文含粗斜/下划线 | 保留首段格式，不强制 H1，不额外插入标题 |
| E02 | H1/H2/H3、正文、连续/中断代码行 | 对应规范 HTML；代码只合并连续 codeLine |
| E03 | 三级有序/无序混合 | 子列表位于父 li 中；编号与 resolver 一致 |
| E04 | 含 `<tag>`、`&`、换行、双空格的代码 | 正确转义并保留代码空白，无 HTML 注入 |
| E05 | 多个行内 marks 重叠 | 合法且确定性的嵌套，不因字体 fallback 丢失语义 |
| E06 | 图文混排、重复引用图片、仅图片 | 正文无 U+FFFC；图文原位混排，重复图片按出现次数保留；清理临时文件后图片仍完整可读 |
| E06b | 附件校验失败、回滚失败、图片槽位缺失 | 不清空草稿；尝试移除未完成笔记，失败则保留图片并提示；缺失图片在创建前拒绝 |
| E07 | 空首段、末尾空段 | 空段按模型输出，name 不传，不生成假标题 |
| E08 | 规范化粘贴的混合字号/等宽 run | 按 D11 已批准规则验收，不能默默丢失或误判为标题 |
| E09 | AppleScript 源串检查 | `make new note` 仅带 body；正确转义引号/反斜线 |
| E10 | Notes 写入成功/拒绝权限/附件失败 | 成功才清空；失败保留草稿；不把编辑器内位置保证扩展到 Notes |

## 9. 性质测试与最终门槛

针对根本风险增加少量性质测试，而非只复写实现逻辑：

- 对含中文、emoji、组合字符、附件和空段的随机文档，合法位置在模型与 UTF-16 范围间往返保持一致。
- 随机执行输入、删除、格式、列表升降级、换行和撤销序列；每一步至少一个段落、层级合法、selection 可定位、附件引用有效。
- 对可撤销事务执行 apply → undo → redo，文档/选区/输入意图恢复到对应值；派生编号与重算结果相同。
- 同一文档的局部投影结果与全量参考投影一致；不同增量编辑路径到达相同语义状态时导出相同 HTML。
- 任意用户文本均只作为文本进入 HTML，不能生成白名单外节点；粘贴和历史回放不调用触发器。

最终至少在 macOS 13 和当前支持的较新系统验证；测试系统、输入法、Xcode 版本和失败复现序列写入结果。长文档滚动/输入使用实测延迟发现问题，不在没有测量时承诺固定性能数字。

发布门槛仍是 Core、AppKit 和关键真机项目全部通过。当前开发版本已替换旧编辑路径，尚未完成的真机项目必须在发布前补验，详见执行结果。
